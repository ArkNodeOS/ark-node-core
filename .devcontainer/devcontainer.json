{
	"name": "ark-node-core",
	"build": {
		"dockerfile": "Dockerfile",
		"context": ".."
	},
	"customizations": {
		"vscode": {
			"extensions": [
				"oven.bun-vscode",
				"EditorConfig.EditorConfig",
				"biomejs.biome"
			]
		}
	},
	"forwardPorts": [3000],
	"postCreateCommand": "bun install && cd src/web && bun install"
}
